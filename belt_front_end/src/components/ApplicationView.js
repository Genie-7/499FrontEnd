import React, {useEffect, useState} from 'react';
import axios from 'axios';

const ApplicationView = () => {
    return(
        <p>
            This is for viewing single applications.
        </p>
    );
}

export default ApplicationView;